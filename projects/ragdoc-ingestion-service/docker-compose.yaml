services:
  ragdoc-db-init:
    restart: 'no'
    build:
      context: .
      dockerfile: ./Dockerfile
    depends_on:
      ragdoc-db:
        condition: service_healthy
    environment:
      DATABASE_URI: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@ragdoc-db:5432/${POSTGRES_DB}
    command: [ "python", "-m", "niagads.ragdoc_ingestion_service.init_schema" ]
  ragdoc-ingestion-service:
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: always
    environment:
      DATABASE_URI: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@ragdoc-db:5432/${POSTGRES_DB}
